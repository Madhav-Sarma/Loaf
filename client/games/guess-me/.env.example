# Client (Vite) environment variables
# Copy to `client/games/guess-me/.env` for local development

# Use VITE_ prefix so Vite exposes these to the client code
# The socket hook reads `VITE_SERVER_URL`:
VITE_SERVER_URL=http://localhost:3001
